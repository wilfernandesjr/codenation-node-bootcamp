const env = process.env.NODE_ENV || 'development'

const cars = {
  findAll: () => {},

  findById: id => {},

  create: data => {},

  update: (data, id) => {},

  delete: id => {}
}


module.exports = { cars }